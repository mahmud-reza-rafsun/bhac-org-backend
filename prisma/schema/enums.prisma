enum Role {
    ADMIN
    MEMBER
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum IdeaStatus {
    PENDING
    APPROVED
    REJECTED
}

enum VoteType {
    UPVOTE
    DOWNVOTE
}

enum ArchiveStatus {
    TRUE
    FALSE
}

enum PaymentStatus {
    PAID
    UNPAID
}

enum NotificationType {
    IDEA_STATUS_UPDATE
    SYSTEM
}
